export enum DeviceType {
  Mobile = 'Mobile',
  Desktop = 'Desktop'
}

export enum ValidType {
  Application = 'application',
  Dataset = 'dataset',
  User = 'user'
}

export enum ValidCount {
  Application = 20,
  Dataset = 50,
  User = 2
}
